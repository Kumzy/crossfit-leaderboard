"""Competitions domain logic."""
from app.domain.competitions import controllers, dependencies, schemas, services, signals, urls

__all__ = ["services", "controllers", "dependencies", "schemas", "signals", "urls"]
